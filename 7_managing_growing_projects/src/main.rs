// This is the crate binary root.
// Rust knows to run main.rs files from the root of a crate. 

// Functions by default are private, meaning code outside of this file cannot reference to it.
fn main() {
  println!("Hello, world!");
}
